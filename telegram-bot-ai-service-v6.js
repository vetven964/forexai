'use strict';
require('dotenv').config();
const TelegramBot=require('node-telegram-bot-api');
const TOKEN=String(process.env.TELEGRAM_TOKEN||'').trim();
const CHAT_ID=String(process.env.TELEGRAM_CHAT_ID||'').trim();
const CORE_URL=String(process.env.VTRADE_CORE_URL||'http://127.0.0.1:10000').replace(/\/$/,'');
const BRIDGE_KEY=String(process.env.TELEGRAM_BRIDGE_API_KEY||process.env.MT5_BRIDGE_API_KEY||'').trim();
const POLL_MS=Math.max(30000,Number(process.env.TELEGRAM_AI_POLL_MS||60000));
const MAX_AGE_SEC=Math.max(5,Number(process.env.TELEGRAM_MAX_PRICE_AGE_SEC||15));
if(!TOKEN||!CHAT_ID){console.warn('[V-ZONEAI TELEGRAM] disabled: missing Telegram credentials');process.exit(0);}
const bot=new TelegramBot(TOKEN,{polling:{interval:3000,autoStart:true,params:{timeout:25}}});
let busy=false,lastSentKey='';
const n=v=>Number.isFinite(Number(v))?Number(v):null;
const F=v=>v==null?'WAIT':Number(v).toFixed(2);
const yes=v=>v===true?'PASS':'WAIT';
function authorityFrom(snapshot){return snapshot?.authority&&typeof snapshot.authority==='object'?snapshot.authority:snapshot;}
async function request(path){const headers={'Cache-Control':'no-cache','X-VTRADE-CLIENT':'vzoneai-canonical-v2'};if(BRIDGE_KEY)headers['X-VTRADE-TELEGRAM-KEY']=BRIDGE_KEY;const r=await fetch(CORE_URL+path,{headers,cache:'no-store'});const d=await r.json().catch(()=>({}));if(!r.ok||d.success===false)throw new Error(d.error||`HTTP ${r.status}`);return d;}
function validOhlc(c){const o=n(c?.o??c?.open),h=n(c?.h??c?.high),l=n(c?.l??c?.low),cl=n(c?.c??c?.close);return [o,h,l,cl].every(Number.isFinite)&&h>=Math.max(o,cl)&&l<=Math.min(o,cl)&&h>=l;}
function canonicalRealCandle(a){const g=a?.realCandleGate||{};const tf=a?.timeframes||{};const core=['M5','M15','H1','H4'];const coreReady=g.coreReady===true||g.ok===true||a.canonicalRealCandle===true;const tfReady=core.every(k=>{const row=tf[k]||{}, gr=g.timeframes?.[k]||{}, count=n(row.bars??row.count??gr.bars??gr.count)??0;return count>=30&&(row.realCandle===true||gr.realCandle===true||row.brokerNative===true||gr.brokerNative===true);});const m5=tf.M5||{};const candle=m5.candle||(Array.isArray(m5.candles)?m5.candles.at(-1):null);const ohlc=validOhlc(candle);const source=String(candle?.source??candle?.provider??m5.source??'').toUpperCase();const native=source===''||source.includes('MT5')||source.includes('BROKER_NATIVE');const explicitReject=a.fakeCandle===true||a.syntheticCandle===true||a.candleFabricated===true||g.synthetic===true||g.fake===true;return !explicitReject&&coreReady&&tfReady&&ohlc&&native;}
async function snapshot(){const d=await request('/api/pre-market/mt5-authoritative');const a=authorityFrom(d);if(!a||typeof a!=='object')throw new Error('Canonical MT5 authority payload missing');const m5=a?.timeframes?.M5||{};const realCandle=canonicalRealCandle(a);const processing=a.processing||{};const gates=a.gates||a.confirmations||{};const merged={...a,realCandle,candleValid:realCandle,fakeCandle:!realCandle,syntheticCandle:!realCandle,price:a.price??a.livePrice,livePrice:a.livePrice??a.price,brokerConnected:a.brokerConnected!==false,priceAgeSec:a.priceAgeSec??a.candleAgeSec,decision:a.decision||{passed:a.signalEligible===true||a.entryReady===true||a.validEntry===true},confirmations:a.confirmations||gates,gates:{...gates,allGatesPassed:gates.allGatesPassed===true||processing.gatesPassed>=Number(processing.gatesRequired||10)},takeProfit:a.takeProfit||a.execution?.tp||[],entry:a.entry??a.execution?.entry,stopLoss:a.stopLoss??a.execution?.sl};return{...d,authority:merged,price:merged.price,quoteConnected:d.quoteConnected!==false||merged.brokerConnected===true,preMarketLoaded:true,mtfCounts:d.mtfCounts||{M5:m5.bars||m5.count||0,M15:a.timeframes?.M15?.bars||0,H1:a.timeframes?.H1?.bars||0,H4:a.timeframes?.H4?.bars||0}};}
function normalize(a){const c=a?.confirmations||a?.gates||{};const signal=['BUY','SELL'].includes(String(a?.signal||'').toUpperCase())?String(a.signal).toUpperCase():'WAIT';const canonicalPassed=a?.decision?.passed===true||a?.confirmations?.allGatesPassed===true||a?.gates?.allGatesPassed===true||a?.signalEligible===true||a?.entryReady===true||a?.validEntry===true;const brokerFresh=a?.brokerConnected!==false&&a?.stalePrice!==true&&(n(a?.priceAgeSec)==null||n(a.priceAgeSec)<=MAX_AGE_SEC);const fakeCandle=a?.fakeCandle===true||a?.syntheticCandle===true||a?.candleFabricated===true||a?.realCandle===false;const realCandle=a?.realCandle===true&&!fakeCandle;const tp=Array.isArray(a?.takeProfit)?a.takeProfit:[];const hasTradeLevels=n(a?.entry)!=null&&n(a?.stopLoss)!=null&&tp.length>=3;const authorized=signal!=='WAIT'&&canonicalPassed&&brokerFresh&&realCandle&&hasTradeLevels;const gates={mtf:c.mtfAligned===true||c.mtfCount>=2,sweep:c.liquiditySweep===true,mss:c.mss===true,bos:c.bos===true,displacement:c.displacement?.confirmed===true||c.displacement===true,fvg:c.freshFvg===true||c.fvg===true,ob:c.freshOb===true||c.orderBlock===true,pd:c.premiumDiscountOk===true,momentum:c.technicalMomentumOk===true,trend:c.trendStrengthOk===true,spread:c.spreadOk===true,realCandle};const required=Number(a?.processing?.gatesRequired||11);const passed=Number.isFinite(Number(a?.processing?.gatesPassed))?Number(a.processing.gatesPassed):Object.values(gates).filter(Boolean).length;const reasons=[];if(signal==='WAIT')reasons.push('signal=WAIT');if(!canonicalPassed)reasons.push('canonical-authority-not-authorized');if(!brokerFresh)reasons.push('broker-freshness');if(!realCandle)reasons.push('REAL-CANDLE-REQUIRED');if(!hasTradeLevels)reasons.push('entry/sl/tp-incomplete');return{a,signal:authorized?signal:'WAIT',rawSignal:signal,authorized,brokerFresh,canonicalPassed,realCandle,fakeCandle,gates,passed,total:required,reasons};}
function transitionLine(d){const t=d?.marketTransition||d?.workflow?.marketTransition||{};const phase=String(t.phase||d?.executionContext||'UNKNOWN').toUpperCase();const friday=d?.fridayCandleRole==='HISTORICAL_REFERENCE'||t.fridayContext===true;const fresh=d?.freshMondayM5===true||t.mondayFreshM5===true;const context=phase==='SUNDAY_PREOPEN'?'SUNDAY PRE-OPEN':phase==='MONDAY_OPEN_WAIT'?'MONDAY OPEN — WAIT FRESH M5':phase==='MONDAY_LIVE_REVALIDATION'?'MONDAY LIVE — FRESH M5 REVALIDATION':phase==='LIVE_MARKET'?'LIVE MARKET':phase;return `🗓️ Context: *${context}* | Friday ${friday?'HISTORY':'LIVE'} | Monday M5 ${fresh?'FRESH':'WAIT'}`;}
function format(x,manual=false,snap=null){const a=x.a,g=x.gates,side=x.signal==='BUY'?'🟢 BUY LONG / BULLISH':x.signal==='SELL'?'🔴 SELL SHORT / BEARISH':'🟡 WAIT';const lines=['🤖 *V-ZoneAI — XAUUSD*',`💰 ${F(a.livePrice??a.price)} | *${side}*`,`📈 *${a.bias||'NEUTRAL'}* | Score *${a.setupScore??a.confidence??0}/100* | Conf *${a.confidence??0}/100*`,transitionLine(snap||a),`🕯️ Real Candle *${x.realCandle?'PASS':'REJECT'}* | Fake Candle *${x.fakeCandle?'REJECT':'BLOCKED'}*`,`🔎 ICT/CRT Authority *${x.passed}/${x.total}*`,`💧 Sweep ${yes(g.sweep)} | MSS ${yes(g.mss)} | BOS ${yes(g.bos)}`,`⚡ Disp ${yes(g.displacement)} | FVG ${yes(g.fvg)} | OB ${yes(g.ob)}`,`🧠 MTF ${yes(g.mtf)} | PD ${yes(g.pd)} | Momentum ${yes(g.momentum)}`,`📍 ADX ${yes(g.trend)} | Spread ${yes(g.spread)}`,`📰 News/Authority *${x.canonicalPassed?'PASS':'WAIT'}*`,`📍 Enter Zone: ${x.authorized?'READY':'WAIT'}`,`🎯 Entry ${F(a.entry)} | SL ${F(a.stopLoss)}`,`🎯 TP1 ${F(a.takeProfit?.[0])} | TP2 ${F(a.takeProfit?.[1])} | TP3 ${F(a.takeProfit?.[2])}`,'',x.authorized?'🔐 *SIGNAL AUTHORIZED — AUTO ORDER OFF*':'⏳ *WAIT — NO ORDER*',`🧠 ${a.decision?.reason||a.trigger||'Canonical authority has not authorized an entry.'}`];if(manual&&x.reasons.length)lines.push(`🔐 Gate reason: ${x.reasons.join(', ')}`);return lines.join('\n');}
async function scan(force=false,manual=false){if(busy)return;busy=true;try{const snap=await snapshot();const a=authorityFrom(snap);const x=normalize(a);console.log(`[V-ZONEAI TELEGRAM] Scan | raw=${x.rawSignal} | final=${x.signal} | bias=${a.bias||'NEUTRAL'} | score=${a.setupScore??a.directionScore??a.confidence??0} | conf=${a.confidence??0} | gates=${x.passed}/${x.total} | realCandle=${x.realCandle} | authority=${x.authorized} | reasons=${x.reasons.join(',')||'none'}`);if(!manual&&!x.authorized){console.log('[V-ZONEAI TELEGRAM] AUTO ALERT SUPPRESSED | ENTRY-ONLY | REAL-CANDLE + AUTHORITY required');return;}const key=x.authorized?`${x.signal}|${a.candleAgeSec}|${a.executionTimeframe}|${a.entry}|${a.stopLoss}|${(a.takeProfit||[]).join(',')}`:`MANUAL|${a.signal}|${a.timestamp}|${a.livePrice}|${x.passed}|${x.reasons.join(',')}`;if(!force&&key===lastSentKey)return;await bot.sendMessage(CHAT_ID,format(x,manual,snap),{parse_mode:'Markdown'});lastSentKey=key;console.log(`[V-ZONEAI TELEGRAM] SENT | signal=${x.signal} | authorized=${x.authorized} | manual=${manual}`);}catch(e){console.warn('[V-ZONEAI TELEGRAM] delivery failed:',e.message);}finally{busy=false;}}
bot.on('polling_error',e=>console.warn('[V-ZONEAI TELEGRAM] polling_error:',e.message));
bot.onText(/^\/signal(?:@\w+)?$/i,async msg=>{if(String(msg.chat.id)!==CHAT_ID)return;await scan(true,true);});
bot.onText(/^\/status(?:@\w+)?$/i,async msg=>{if(String(msg.chat.id)!==CHAT_ID)return;try{const d=await snapshot();const a=authorityFrom(d)||{};const x=normalize(a);await bot.sendMessage(msg.chat.id,`🤖 *V-ZoneAI Telegram Bot*\nBot: *ONLINE*\nCore: *${d.preMarketLoaded?'READY':'WAIT'}*\nMT5: *${d.quoteConnected?'READY':'WAIT'}*\nReal Candle: *${x.realCandle?'PASS':'REJECT'}*\nPrice: *${F(d.price??a.livePrice??a.price)}*\nAuthorization: *${x.authorized?'AUTHORIZED':'WAIT'}*\nSignal: *${x.signal}*\nGates: *${x.passed}/${x.total}*\n${transitionLine(d)}`,{parse_mode:'Markdown'});}catch(e){await bot.sendMessage(msg.chat.id,`⚠️ *STATUS unavailable*\n${e.message}`,{parse_mode:'Markdown'});}});
setInterval(()=>scan(false,false),POLL_MS);scan(false,false);
