name: V TRADE AI New Website Smoke

on:
  push:
    branches: [main]
    paths:
      - 'vtrade-new/**'
      - 'scripts/vtrade-new-smoke.mjs'
      - 'tests/logic-v4-sr-liquidity.test.js'
      - 'logic-v4-execution.js'
      - '.github/workflows/vtrade-new-smoke.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: S/R + Liquidity execution guard tests
        run: node tests/logic-v4-sr-liquidity.test.js
      - name: Standalone new website smoke test
        run: node scripts/vtrade-new-smoke.mjs
